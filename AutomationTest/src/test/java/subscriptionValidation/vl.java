package subscriptionValidation;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class vl {

	
	/*
	 * while (true) { try { WebElement $AlertManageruser = driver .findElement(By.
	 * xpath("//div[text()='Email sent to Email Id with Activation link code.']"));
	 * // (//div[@id='bootstrap-alert'])[1]/div[1]/div if
	 * (!$AlertManageruser.getText().isEmpty() && $AlertManageruser.isDisplayed()) {
	 * System.out.println($AlertManageruser.getText()); // break; } } catch
	 * (Exception e) { // TODO: handle exception } }
	 * 
	 * 
	 * 
	 * 
	 * 
	 * //dc
	 * 
	 * while (true) { try { WebElement $AlertCancel = driver
	 * .findElement(By.xpath("//div[text()='Your Subscription has been cancelled']")
	 * ); if ($AlertCancel.isDisplayed()) { break;
	 * 
	 * } } catch (Exception e) { // TODO: handle exception } }
	 * 
	 * 
	 * //ac
	 * 
	 * 
	 * 
	 * while (true) { try {
	 * 
	 * WebElement $Alertactive =
	 * driver.findElement(By.xpath("//div[text()='User account is activated']")); if
	 * ($Alertactive.isDisplayed()) {
	 * 
	 * break; } } catch (Exception e) { // TODO: handle exception } }
	 * 
	 */
	
	
	
	
	
}
